---
// src/pages/404.astro
import Button from '../components/atoms/Button.astro';
import Heading from '../components/atoms/Heading.astro';
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - Página no encontrada | Beauty Salon Template">
  <section
    class="relative flex min-h-[80vh] flex-col items-center justify-center overflow-hidden px-4 text-center"
  >
    <div
      class="animate-blob absolute left-10 top-1/4 h-64 w-64 rounded-full bg-pink-200 opacity-30 mix-blend-multiply blur-3xl filter"
    >
    </div>
    <div
      class="animate-blob animation-delay-2000 absolute right-10 top-1/3 h-64 w-64 rounded-full bg-purple-200 opacity-30 mix-blend-multiply blur-3xl filter"
    >
    </div>

    <div class="relative z-10 flex max-w-2xl flex-col items-center">
      <div class="relative mb-8">
        <h1
          class="select-none font-serif text-[150px] font-bold leading-none text-glam-navy opacity-10 md:text-[200px]"
        >
          404
        </h1>

        <div class="absolute inset-0 flex items-center justify-center gap-8">
          <svg
            class="animate-float h-24 w-24 -rotate-12 transform text-pink-500 drop-shadow-xl"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <circle cx="6" cy="6" r="3"></circle>
            <circle cx="6" cy="18" r="3"></circle>
            <line x1="20" y1="4" x2="8.12" y2="15.88"></line>
            <line x1="14.47" y1="14.48" x2="20" y2="20"></line>
            <line x1="8.12" y1="8.12" x2="12" y2="12"></line>
          </svg>

          <svg
            class="animate-float-delayed h-20 w-20 rotate-12 transform text-glam-navy drop-shadow-xl"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M14 2v2"></path>
            <path d="M10 2v2"></path>
            <line x1="18" y1="2" x2="18" y2="4"></line>
            <line x1="6" y1="2" x2="6" y2="4"></line>
            <rect x="2" y="4" width="20" height="16" rx="2"></rect>
          </svg>
        </div>
      </div>

      <Heading tagName="h2" size="xl" align="center" color="navy" className="mb-4">
        ¡Ups! <span class="italic text-pink-500">Un mal corte...</span>
      </Heading>

      <p class="mb-8 max-w-lg text-lg text-glam-gray md:text-xl">
        Parece que la página que buscas se ha cortado demasiado o nunca existió. No te preocupes,
        aquí reparamos cualquier estilo.
      </p>

      <div class="flex flex-col gap-4 sm:flex-row">
        <Button href="/" variant="primary" className="shadow-lg shadow-pink-200/50">
          Volver al Inicio
        </Button>

        <Button href="/servicios" variant="secondary"> Ver Servicios </Button>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Animación suave de flotación */
  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(-12deg);
    }
    50% {
      transform: translateY(-10px) rotate(-12deg);
    }
  }
  @keyframes float-delayed {
    0%,
    100% {
      transform: translateY(0) rotate(12deg);
    }
    50% {
      transform: translateY(-10px) rotate(12deg);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  .animate-float-delayed {
    animation: float-delayed 7s ease-in-out infinite;
  }

  /* Animación para las manchas de fondo */
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }
  .animate-blob {
    animation: blob 10s infinite;
  }
  .animation-delay-2000 {
    animation-delay: 2s;
  }
</style>
