---
import Button from 'src/components/atoms/Button.astro';
import Heading from 'src/components/atoms/Heading.astro';
import ServiceList from 'src/components/organisms/ServiceList.astro';
import { siteData } from 'src/data/siteData';
import Layout from 'src/layouts/Layout.astro';

const { priceList, info } = siteData;
---

<Layout title="Servicios y Precios | Beauty Salon Template">
  <header class="bg-pink-50 px-4 py-20 text-center">
    <Heading tagName="h1" size="xl" align="center" className="mb-4">
      Menú de <span class="italic text-pink-500">Servicios</span>
    </Heading>

    <p class="mx-auto max-w-2xl text-lg text-glam-gray">
      Profesionalismo y transparencia. Cuidamos tu cabello con los mejores productos.
    </p>
  </header>

  <section class="container mx-auto px-4 py-16">
    <div
      class="mx-auto mb-12 flex max-w-5xl items-start gap-4 rounded-r-lg border-l-4 border-yellow-400 bg-yellow-50 p-4"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        class="mt-0.5 h-6 w-6 flex-shrink-0 text-yellow-600"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <div>
        <h4 class="text-sm font-bold uppercase tracking-wide text-glam-navy">
          Política de Precios
        </h4>
        <p class="mt-1 text-sm text-glam-gray">
          Los valores expresados en coloración y tratamientos son <strong
            >precios base estipulados</strong
          >. El costo final puede variar dependiendo de la longitud, volumen y la cantidad de gramos
          de producto requeridos para lograr el resultado deseado. Se realizará un presupuesto
          exacto antes de comenzar el trabajo.
        </p>
      </div>
    </div>

    <div class="mx-auto grid max-w-6xl grid-cols-1 gap-x-24 gap-y-12 lg:grid-cols-2">
      {
        priceList.map((category) => (
          <ServiceList category={category.category} items={category.items} />
        ))
      }
    </div>

    <div class="mt-16 border-t border-gray-100 pt-12 text-center">
      <h3 class="mb-4 font-serif text-2xl text-glam-navy">¿Dudas con tu presupuesto?</h3>
      <p class="mb-8 text-gray-500">
        Envíanos una foto de tu cabello actual y lo que deseas lograr para una asesoría aproximada.
      </p>
      <Button href={info.whatsappLink} variant="primary"> Consultar por WhatsApp </Button>
    </div>
  </section>
</Layout>
